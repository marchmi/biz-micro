# biz-micro 脚手架工具说明文档

## 一、工具概述

biz-micro 是一个基于 Node.js 的微前端项目脚手架工具，旨在帮助开发者快速初始化 qiankun 和 wujie 两种主流微前端框架的项目结构。该工具采用 TypeScript 开发，使用 Commander.js 作为命令行解析框架，结合 Inquirer.js 实现交互式命令行界面，通过 fs-extra 进行文件系统操作。

本脚手架工具的核心价值在于标准化微前端项目的创建流程，减少重复性配置工作，并确保所有生成的项目都遵循最佳实践。通过预定义的模板系统，开发者只需通过简单的命令交互，即可生成包含完整配置、状态管理、路由配置和组件库的微前端项目结构。

工具采用模块化设计，主要分为命令定义层、模板系统层和文件操作层三个部分。命令定义层负责解析用户输入的命令和参数，模板系统层管理项目模板的存储和复制逻辑，文件操作层则负责实际的目录创建、文件读写和环境变量配置等操作。这种分层设计使得工具具有良好的可扩展性，便于后续添加新的命令或模板。

## 二、实现原理

### 2.1 架构设计

脚手架工具的整体架构遵循分层设计原则，从上到下依次为表现层、业务逻辑层和数据访问层。表现层使用 Commander.js 框架定义命令行接口，支持多种子命令的定义和参数传递；业务逻辑层封装了项目创建、初始化和子应用添加的核心流程；数据访问层则利用 fs-extra 库提供的高级文件系统操作接口，实现模板复制、配置文件修改等功能。

工具的入口文件为 `bin/biz-micro.ts`，该文件负责初始化命令行程序、定义所有可用命令、加载模板配置，并启动命令解析流程。当用户执行 `biz-micro` 相关命令时，程序会根据用户输入的命令名称和参数，调用相应的处理函数完成指定操作。整个流程采用异步编程模式，利用 TypeScript 的 async/await 语法确保操作的有序执行和错误的有效捕获。

模板系统采用目录结构存储，每个模板对应一个独立的目录，目录名称遵循 `{framework}-{appType}-{bundler}` 的命名规范。例如，`qiankun-main-vite` 表示 qiankun 框架的主应用模板，使用 Vite 作为构建工具。模板目录中包含了完整的项目结构，涵盖配置文件、源码目录、静态资源等所有必要文件。

### 2.2 命令处理流程

当用户执行 `biz-micro` 命令时，程序首先通过 Commander.js 解析命令行参数，确定用户要执行的子命令及其参数。解析完成后，程序调用对应子命令的处理函数，进入业务逻辑处理阶段。以 `create` 命令为例，完整的处理流程包括：启动交互式问答收集用户需求、验证输入数据的有效性、根据收集的信息生成项目配置、复制模板文件到目标目录、修改项目配置文件、更新环境变量文件，最后输出操作结果和使用指南。

错误处理是命令处理流程中的重要环节。工具在每个可能抛出异常的操作外部包裹 try/catch 块，确保用户能够获得清晰的错误提示信息。错误信息会经过格式化处理，使用 chalk 库为不同级别的信息添加颜色标识，帮助用户快速定位问题。当发生严重错误时，程序会主动退出并返回非零退出码，便于在脚本环境中判断命令执行状态。

### 2.3 模板渲染机制

模板系统在设计时采用了「静态模板+动态配置」的混合模式。模板目录中包含的文件大部分是静态内容，这些文件直接复制到目标项目目录中，不需要任何修改。少部分文件需要根据用户输入进行动态修改，主要包括 package.json 中的项目名称和描述，以及 .env.development 中的端口配置。

模板复制使用 `fs.copy()` 方法实现，该方法能够完整保留文件的权限信息和目录结构。复制完成后，程序会读取目标目录下的 package.json 文件，使用 `fs.readJSON()` 方法解析为 JavaScript 对象，修改其中的 name 和 description 字段后，再通过 `fs.writeJSON()` 方法写回文件。这种方式比直接操作字符串更加安全可靠，能够避免配置文件格式损坏的风险。

环境变量文件的处理采用追加模式。程序首先检查目标目录是否存在 .env.development 文件，如果存在则读取其内容，然后在文件头部插入新的端口配置。如果文件不存在，则创建新文件并写入端口配置。这种处理方式确保了环境变量配置的可追溯性，用户可以清晰地看到哪些配置是脚手架自动添加的。

## 三、核心模块说明

### 3.1 入口模块

入口模块是整个脚手架工具的起点，位于 `bin/biz-micro.ts` 文件。该模块的主要职责包括：导入所有必要的依赖库、定义模板配置数据、声明所有命令的处理函数、配置 Commander.js 命令行程序、启动命令解析流程。

模块首先导入了六个核心依赖库。path 库用于路径拼接和解析，Commander.js 用于命令行参数解析，Inquirer.js 用于交互式问答，fs-extra 用于文件系统操作，chalk 用于终端输出着色，ora 用于显示加载动画。这些依赖库共同构成了脚手架工具的功能基础。

模板配置数据 `TEMPLATES` 是一个数组常量，定义了当前工具支持的所有模板类型。每种模板包含四个属性：name 表示模板唯一标识，description 表示模板的描述信息，framework 表示所属的微前端框架，type 表示应用类型（主应用或子应用）。这个配置数据不仅用于 list 命令的展示，也被 create、init、add 等命令用于模板路径的构建。

### 3.2 命令定义

脚手架工具目前支持四个核心命令：list、create、init 和 add。每个命令都通过 Commander.js 的链式 API 进行定义，包括命令名称、命令描述和动作处理函数。命令定义遵循统一的模式：使用 `.command()` 方法声明命令名称，使用 `.description()` 添加帮助文档，使用 `.action()` 绑定处理函数。

list 命令用于展示所有可用的微前端模板。处理函数 `runList()` 会先筛选出 qiankun 和 wujie 两种框架的模板，然后在终端绘制 ASCII 艺术风格的表格边框，将模板信息格式化输出。表格包含三列：模板名称、应用类型和模板描述。使用说明部分列出了所有可用命令的示例，帮助新用户快速上手。

create 命令用于交互式创建微前端应用。处理函数 `runCreate()` 使用 Inquirer.js 发起一系列问答，问题包括：应用类型（主应用或子应用）、微前端框架（仅主应用需要选择）、构建工具、项目名称和项目描述。根据用户的回答，程序确定要使用的模板名称，然后调用 `generateProject()` 函数完成项目生成。创建完成后，程序会输出后续操作指南，包括安装依赖和启动开发服务器的步骤。

init 命令用于快速初始化主应用。与 create 命令相比，init 命令简化了交互流程，不需要用户选择应用类型（固定为主应用），同时项目名称固定为 main-app。这种设计方便用户快速搭建主应用骨架，后续可以在此基础上进行定制开发。

add 命令用于添加子应用。处理函数 `runAdd()` 收集子应用名称、开发端口和构建工具三个信息。子应用名称用于生成目录名称和 package.json 中的项目名，开发端口写入 .env.development 文件的 VITE_APP_PORT 变量，构建工具决定使用的模板类型。添加完成后，程序会输出子应用的访问地址，提示用户需要将子应用注册到主应用中。

### 3.3 项目生成模块

项目生成模块包含三个核心函数：`generateProject()`、`updatePackageJson()` 和 `updateEnvFile()`。这些函数封装了项目文件操作的具体细节，对外提供简洁的调用接口。

`generateProject()` 函数是项目生成的入口函数，接收一个包含所有配置信息的对象参数。函数首先根据参数构建模板路径和目标路径，然后检查目标目录是否已存在，如果存在则抛出错误。接着使用 `fs.copy()` 方法将模板目录复制到目标位置，复制完成后依次调用 `updatePackageJson()` 和 `updateEnvFile()` 修改项目配置，最后在终端输出项目创建成功的提示信息。

`updatePackageJson()` 函数负责修改 package.json 文件。函数读取目标目录下的 package.json 文件，将其解析为 JavaScript 对象后，修改 name 字段为用户输入的项目名称，description 字段为用户输入的项目描述。修改完成后，使用 JSON 格式化写入文件，保持良好的可读性。

`updateEnvFile()` 函数负责配置开发环境的端口变量。函数首先检查目标目录是否存在 .env.development 文件，如果存在则读取其内容。然后将新的端口配置插入到文件开头，使用 `VITE_APP_PORT={port}` 的格式。新的配置覆盖原有的端口设置，但保留文件中可能存在的其他配置项。

### 3.4 类型定义

为了保证代码的类型安全和可维护性，脚手架工具为关键数据结构和函数参数定义了 TypeScript 类型。主要的类型定义包括 `GenerateProjectOptions` 接口和 `TEMPLATES` 数组的类型注解。

`GenerateProjectOptions` 接口定义了 `generateProject()` 函数的参数结构，包含五个必填字段和一个可选字段。必填字段包括 appType（应用类型）、framework（框架名称）、bundler（构建工具）、projectName（项目名称）、description（项目描述），可选字段 port（端口号）用于子应用的端口配置。这些类型定义使得函数调用时能够获得良好的类型检查和代码补全支持。

TEMPLATES 数组的类型注解确保了模板配置的一致性。数组中的每个元素都是一个包含 name、description、framework、type 四个字符串属性的对象，TypeScript 编译器会根据类型注解检查数组元素的完整性，避免因配置错误导致的运行时问题。

## 四、命令详解

### 4.1 list 命令

list 命令用于列出所有可用的微前端模板，不接受任何参数。执行该命令后，终端会显示一个格式化的模板列表，包括每个模板的名称、类型和描述信息。列表按照框架分组显示，首先展示 qiankun 框架的模板，然后展示 wujie 框架的模板。每个框架内部又按照主应用和子应用进行细分。

使用 `biz-micro list` 或 `biz-micro`（不带参数）可以执行此命令。输出信息分为三个部分：模板列表、使用说明和分隔线。模板列表使用 ASCII 字符绘制了美观的表格边框，不同类型的信息使用不同的颜色标识，其中青色用于表格框架，灰色用于使用说明，白色用于命令示例。

该命令的主要用途是帮助用户了解当前脚手架工具支持哪些模板，以及各模板的命名规范。在创建新项目之前，建议先执行此命令确认模板名称，然后在 create 或 init 命令中使用正确的模板参数。

### 4.2 create 命令

create 命令用于交互式创建微前端项目，是脚手架工具最核心的功能。执行该命令后，程序会依次发起五个问题的问答，用户需要逐个回答这些问题来完成项目配置。

第一个问题要求用户选择应用类型，可选值为「主应用」或「子应用」。主应用用于管理和加载子应用，子应用被主应用加载和渲染。第二个问题要求用户选择微前端框架，可选值为「qiankun」或「wujie」，这个问题只在选择主应用时出现。第三个问题要求用户选择构建工具，可选值为「Vite」或「Webpack」，Vite 是推荐的现代构建工具，提供了更快的开发体验。第四个问题要求用户输入项目名称，用于创建项目目录和设置 package.json 中的 name 字段。第五个问题要求用户输入项目描述，用于设置 package.json 中的 description 字段。

所有问题都包含输入验证逻辑。项目名称不能为空，项目描述可以为空，端口号必须是有效范围内的数字。验证失败时会显示相应的错误提示，用户需要重新输入有效值。配置收集完成后，程序会自动执行项目生成流程，用户只需等待操作完成。

### 4.3 init 命令

init 命令用于快速初始化主应用，适合需要快速搭建主应用骨架的场景。与 create 命令相比，init 命令省略了应用类型选择（固定为主应用）和项目名称输入（固定为 main-app），用户只需要选择框架和构建工具两个配置项。

执行 `biz-micro init` 命令后，程序会发起两个问题的问答。第一个问题要求用户选择微前端框架，可选值为「qiankun」或「wujie」。第二个问题要求用户选择构建工具，可选值为「Vite」或「Webpack」。根据用户的回答，程序会自动创建名为 main-app 的项目目录，并在其中生成对应框架的主应用模板。

初始化完成后，终端会显示项目创建成功的提示信息和后续操作指南。用户可以直接进入 main-app 目录，执行 npm install 安装依赖，然后使用 npm run dev 启动开发服务器。这种简化的流程非常适合快速原型验证和主应用骨架搭建。

### 4.4 add 命令

add 命令用于添加新的子应用，是主应用开发过程中的常用操作。执行该命令后，程序会发起三个问题的问答，收集子应用的配置信息。

第一个问题要求用户输入子应用名称，该名称会用于创建子应用目录和设置 package.json 中的 name 字段。名称不能为空，建议使用英文且不包含空格。第二个问题要求用户输入子应用的开发端口号，默认值为 3001，端口号必须在 1 到 65535 的有效范围内。不同的子应用需要使用不同的端口号，以避免端口冲突。第三个问题要求用户选择构建工具，可选值为「Vite」或「Webpack」。

子应用创建完成后，终端会显示子应用的访问地址（http://localhost:{port}），并提示用户需要将子应用注册到主应用中。这是非常重要的一步，因为脚手架工具只负责创建子应用的结构，注册的逻辑需要用户在主应用中手动完成。对于 qiankun 框架，需要在主应用的 registerMicroApps 函数中注册子应用；对于 wujie 框架，需要在主应用的 apps 配置中添加子应用信息。

## 五、模板系统

### 5.1 模板目录结构

脚手架工具的模板存储在 `templates` 目录下，每个模板对应一个独立的子目录。目前支持的模板包括四个：qiankun-main-vite（qiankun 主应用模板）、qiankun-sub-vite（qiankun 子应用模板）、wujie-main-vite（wujie 主应用模板）、wujie-sub-vite（wujie 子应用模板）。

每个模板目录都包含完整的项目结构。以 qiankun-main-vite 模板为例，目录结构包括：package.json（项目配置）、vite.config.ts（Vite 配置）、tsconfig.json（TypeScript 配置）、index.html（入口 HTML）、src/main.ts（入口 JS）、src/App.vue（根组件）、src/router/index.ts（路由配置）、src/stores/**/*.ts（Pinia 状态管理）、src/views/**/*.vue（视图组件）、public/（静态资源）、.env.development（开发环境变量）。

模板目录的设计遵循 Vue 3 项目的最佳实践，使用 Composition API 编写组件代码，使用 TypeScript 进行类型检查，使用 Pinia 进行状态管理，使用 Vue Router 进行路由管理，使用 Element Plus 作为 UI 组件库。这种标准化的项目结构有助于团队保持代码风格的一致性，降低项目交接和维护的成本。

### 5.2 模板定制方法

如果需要修改现有模板或添加新的模板，需要遵循一定的规范。修改现有模板时，直接编辑 templates 目录下对应模板目录中的文件即可。例如，如果需要在 qiankun 主应用模板中添加一个新的公共组件，只需在模板目录的 src/components 目录下创建组件文件。

添加新模板需要完成三个步骤。第一步是创建模板目录，目录名称必须遵循 `{framework}-{appType}-{bundler}` 的命名规范，例如 myframework-main-webpack。第二步是填充模板目录，将完整的项目文件复制到新目录中，并根据模板的特点进行必要的修改。第三步是更新 `biz-micro.ts` 文件中的 TEMPLATES 数组，添加新模板的配置信息，包括 name、description、framework 和 type 四个字段。

模板中的占位符需要在运行时动态替换。目前支持的占位符包括：`{projectName}` 在 package.json 中替换为用户输入的项目名称，`{description}` 在 package.json 中替换为用户输入的项目描述，`{port}` 在 .env.development 中替换为用户输入的端口号。如果需要支持更多的占位符，需要在 `updatePackageJson()` 或 `updateEnvFile()` 函数中添加相应的处理逻辑。

### 5.3 框架特定配置

qiankun 和 wujie 两个框架在微前端的实现方式上有显著差异，模板中也体现了这些差异。qiankun 框架采用 JS 沙箱和快照的方式实现子应用隔离，主应用通过 import-html-entry 加载子应用，使用 registerMicroApps API 注册子应用，使用 start 函数启动微前端系统。wujie 框架采用 WebComponent 和 iframe 的方式实现子应用隔离，主应用使用 WujieVue 组件加载子应用，通过 apps 配置数组定义子应用信息。

在主应用模板中，qiankun 主应用包含一个 apps.ts 文件用于定义子应用注册信息，包含 activeRule、name、entry、container、props 等配置项。wujie 主应用在 App.vue 中直接使用 WujieVue 组件，并在组件的 apps 属性中定义子应用配置。两种框架的配置方式各有特点，选择时需要根据团队的技术栈和业务需求进行决策。

子应用模板的差异主要体现在入口文件和生命周期处理上。qiankun 子应用需要导出 bootstrap、mount、unmount 三个生命周期函数，mount 函数接收主应用传递的 props，props 中包含自定义 qs 对象用于主子应用通信。wujie 子应用不需要特殊的生命周期导出，但需要使用 window.__POWERED_BY_WUJIE__ 判断当前是否在 wujie 环境中运行，以便进行特殊处理。

## 六、维护指南

### 6.1 代码结构

维护脚手架工具首先需要熟悉其代码结构。核心代码位于 `bin` 目录下，入口文件为 `biz-micro.ts`，这是整个工具的逻辑起点。`templates` 目录包含所有项目模板，每个模板是一个完整的子目录结构。`package.json` 文件定义了项目的依赖和脚本，`tsconfig.json` 文件配置了 TypeScript 编译选项。

代码维护时需要注意模块边界。biz-micro.ts 文件负责命令定义和流程控制，不应该包含过多的业务逻辑。文件操作逻辑封装在独立的函数中，通过函数调用链完成复杂的操作流程。这种设计使得代码职责清晰，便于单元测试和问题定位。

类型定义是代码质量的重要保障。所有函数参数和返回值都应该有明确的类型注解，新增的数据结构需要创建对应的接口或类型别名。类型注解不仅帮助 TypeScript 编译器发现潜在错误，也为 IDE 提供了代码补全和类型检查的支持，提高开发效率。

### 6.2 依赖管理

脚手架工具的依赖分为两类：dependencies 和 devDependencies。dependencies 包含运行时依赖，主要有 commander（命令行解析）、inquirer（交互式问答）、fs-extra（文件系统操作）、chalk（终端着色）、ora（加载动画）五个库。devDependencies 包含开发时依赖，主要有 typescript（TypeScript 编译器）、ts-node（直接运行 TypeScript）、@types/*（类型定义包）以及测试相关的 jest 和 ts-jest。

添加新依赖时，需要使用 `npm install <package-name>` 命令，该命令会自动更新 package.json 文件。如果添加的是运行时依赖，需要确保依赖库在 Node.js 环境中能够正常运行，避免使用仅在浏览器环境可用的库。如果添加的是开发时依赖，需要使用 `--save-dev` 参数将其添加到 devDependencies 中。

依赖版本管理遵循语义化版本规范。使用 `^` 前缀允许次要版本和补丁版本的自动更新，使用 `~` 前缀仅允许补丁版本的自动更新，使用精确版本号则禁止任何自动更新。对于生产环境使用的依赖，建议使用 `^` 前缀以获取安全修复，同时避免引入可能破坏兼容性的重大变更。

### 6.3 测试方法

脚手架工具内置了 Jest 测试框架，测试文件位于项目根目录或专门的 tests 目录下。运行测试使用 `npm test` 命令，该命令会执行所有 *.test.ts 或 *.spec.ts 文件中的测试用例。

测试覆盖范围应该包括命令解析、文件操作和错误处理三个层面。命令解析测试验证不同参数组合能够触发正确的处理函数；文件操作测试验证模板复制、配置修改等功能能够正确执行；错误处理测试验证异常情况能够得到适当的处理和反馈。

新增功能时应该同步添加相应的测试用例。测试用例应该覆盖正常流程和边界情况，确保功能在各种输入条件下都能正确运行。测试通过后才能将代码合并到主分支，保持测试覆盖率在一个合理的水平。

### 6.4 发布流程

脚手架工具的发布包括版本号更新、构建和发布三个步骤。版本号遵循语义化版本规范，重大功能更新递增主版本号，常规功能更新递增次版本号，缺陷修复递增补丁号。版本号更新后需要修改 package.json 中的 version 字段，并提交一条标注版本号的 git 提交记录。

构建使用 `npm run build` 命令，该命令会调用 tsc 编译器将 TypeScript 代码编译为 JavaScript 代码。编译产物输出到 bin 目录，包括 *.js 主文件、*.d.ts 类型声明文件和 *.js.map 源映射文件。编译过程中如果出现错误，需要修复相应的代码问题后才能继续发布。

发布使用 `npm publish` 命令，将包发布到 npm 仓库。发布前需要确保用户已经登录 npm 账号（使用 `npm login` 命令登录），并且包名在 npm 仓库中没有冲突。如果使用私有仓库，需要配置相应的 registry 地址。发布完成后，可以通过 `npm view biz-micro` 命令查看包信息，通过 `npm install -g biz-micro` 全局安装后使用。

### 6.5 常见问题处理

运行时出现 `spawn ts-node ENOENT` 错误，表示系统中缺少 ts-node 依赖。解决方法是在项目目录执行 `npm install ts-node`，然后重新运行命令。该错误通常发生在首次克隆项目或依赖被意外删除后。

TypeScript 编译出现 `No inputs were found in config file` 错误，表示 include 路径配置不正确。检查 tsconfig.json 文件中的 include 数组，确保路径模式能够匹配到源码文件。例如，如果源码位于 bin 目录，include 应该配置为 `["bin/**/*"]`。

运行时出现 `Cannot use import statement outside a module` 错误，表示编译输出的模块格式与运行环境不匹配。检查 tsconfig.json 中的 module 配置，确保使用的是 CommonJS（用于 Node.js 环境）或 ES2020（用于 ESM 环境）。如果修改了 module 配置，需要重新编译后才能生效。

运行时出现 `Identifier '__dirname' has already been declared` 错误，表示自定义的 __dirname 变量与 Node.js 内置变量冲突。CommonJS 格式的模块已经内置了 __dirname 变量，不需要也不应该重新定义。如果需要获取当前目录，应该直接使用 Node.js 提供的 __dirname，或者使用 `path.resolve()` 方法动态计算。
